Rename password reset tokens to password resets
Make all queries respect expiration dates
Correct error handling
Quality testing setup w/ testify with db wipe
Make sure that certain commands cant be executed on guest users
Respect GRPC enums, and handle not found cases in repo instead of returning SQL errors
Build cleaner
Allow configuration for commands, fill out cobra text, allow secret to be set
Create DB bash script
Update README
Write a ton of tests
Redis session caching
Reminder in README to store token in cookie, not in local storage, otherwise venerable to session hijack
Docker build for go, google search it